// Copy this file to config.js and fill in your values.
// For Vercel, use PORTAL_ORGANIZATION_ID in env and scripts/write-config.js at build.
window.PORTAL_CONFIG = {
  supabaseUrl: 'https://YOUR_PROJECT_REF.supabase.co',
  supabaseAnonKey: 'YOUR_ANON_KEY',
  organizationId: 'YOUR_ORG_UUID'  // Our org — portal is our landing page; leads are scoped here.
};
