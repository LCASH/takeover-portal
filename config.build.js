// Placeholder – overwritten by "npm run build" from env (SUPABASE_URL, SUPABASE_ANON_KEY, PORTAL_ORGANIZATION_ID).
// This file is committed so Vercel includes it in the deployment; the build step replaces it with real values.
window.PORTAL_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
  organizationId: null
};
