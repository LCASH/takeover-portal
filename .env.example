# Copy to .env and fill in (or set these in Vercel → Environment Variables).
# .env is gitignored – never commit real keys. Build writes config.build.js from these.
SUPABASE_URL=
SUPABASE_ANON_KEY=
PORTAL_ORGANIZATION_ID=
